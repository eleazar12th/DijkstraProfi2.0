export default class State {
    constructor(currDist, nodeColors, edgeColors, description) {
        this.currDist = currDist;
        this.nodeColors = nodeColors;
        this.edgeColors = edgeColors;
        this.description = description;
    }
}